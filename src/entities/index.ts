import { PRODUCTS_ENTITIES } from '@entities/products';

import { IMAGE_ENTITIES } from './images';

export const ENTITIES = [...IMAGE_ENTITIES, ...PRODUCTS_ENTITIES];

export const SUBSCRIBERS = [];
